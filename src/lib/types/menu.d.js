/**
 * @typedef Menu
 * @prop {Data[]} menu
 * @prop {User} user
 * 
 * 
 * @typedef User
 * @prop {number} id
 * @prop {string} name
 * @prop {string} email
 * @typedef Data
 * @prop {number} id
 * @prop {string} name
 * @prop {string} url
 * @prop {number} parent_id
 * @prop {Data[]?} children
 */

module.exports = {}